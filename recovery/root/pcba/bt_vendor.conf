# UART device port where Bluetooth controller is attached
# modified by lifeizhu begin
UartPort = /dev/ttyS9
# modified by lifeizhu end

# Firmware patch file location
FwPatchFilePath = /vendor/etc/firmware/
